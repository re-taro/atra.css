import { style } from '@vanilla-extract/css';

export const fallback = style({
  height: '100vh',
});

export const container = style({
  maxWidth: '980px',
  margin: '5vh auto',
  padding: '0 5vw',
});
